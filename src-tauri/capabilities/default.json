{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main Yapping window and the recording overlay",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
